# Backend API URL — change to your deployed Railway URL in production
NEXT_PUBLIC_API_URL=http://localhost:5000

# App config
NEXT_PUBLIC_BASE_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=MERNShop

# Skip t3-env validation during builds (set to true only if needed)
SKIP_ENV_VALIDATION=false
